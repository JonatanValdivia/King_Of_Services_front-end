import { ContainerCards, SectionCards } from "./style";

function Cards() {
    return(
        <ContainerCards>
            <SectionCards>
                
            </SectionCards>
        </ContainerCards>
    );
}

export default Cards;